const express = require('express');
const AirlineController = require('../controllers/CustomizeAirlineDBController');

const router = express.Router();

//'/airline/create before'
// router.get('/customize', AirlineController.customize);

module.exports = router;
